pub fn array_main() {
    //array = contiguous memory space
    //ungrowable
    //a is not an 'array' as the computer sees it, its here
    //for representation purposes
    let a = ["hello", "world"];
}